import 'package:flutter/material.dart';

class SearchCharacter extends StatelessWidget {
  const SearchCharacter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Center(
        child: Text('Hello Mundo Swearch'),
      ),
    );
  }
}
